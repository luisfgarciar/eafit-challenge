# NOTE: Do NOT commit real values here.
# Run deploy.sh — it generates this secret automatically from ai-chatbot/.env
#
# To create manually:
#   echo -n "your-value" | base64
#
apiVersion: v1
kind: Secret
metadata:
  name: chatbot-secrets
  namespace: eafit-luisgarcia
type: Opaque
data:
  ANTHROPIC_API_KEY: CHANGE_ME_BASE64
  POSTGRES_PASSWORD: CHANGE_ME_BASE64
  VS_AGENT_STATS_PASSWORD: CHANGE_ME_BASE64
